# Supabase project — copy to `.env` and fill in (never commit `.env`).
# Project ref: wafubtahfwmlbxitbkrl
SUPABASE_URL=https://wafubtahfwmlbxitbkrl.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Postgres connection (Connect button → URI; put your DB password in).
# Use the direct connection (port 5432) for migrations; the pooler (6543) at runtime.
DATABASE_URL=
